<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="io.github.sztuwebclub.webcheckin.mapper.userMapper" >
    <select id="getuserTask" resultType="io.github.sztuwebclub.webcheckin.pojo.Task">
        SELECT t.task_id, t.task_name, t.description, ut.is_completed
        FROM tasks t
                 JOIN user_tasks ut ON t.task_id = ut.task_id
        WHERE ut.user_id = #{userId}

    </select>
    <select id="getusercheckin" resultType="io.github.sztuwebclub.webcheckin.pojo.user">
        SELECT t.task_id, t.task_name, t.description, ut.is_completed
        FROM tasks t
                 JOIN user_tasks ut ON t.task_id = ut.task_id
        WHERE ut.user_id = #{userId}

    </select>
    <select id="getuserCheckInRecords" resultType="io.github.sztuwebclub.webcheckin.pojo.Task">
        SELECT t.task_id, t.task_name, t.description, ut.is_completed
        FROM tasks t
                 JOIN user_tasks ut ON t.task_id = ut.task_id
        WHERE ut.user_id = #{userId}

    </select>
    <select id="getuserattend-records" resultType="io.github.sztuwebclub.webcheckin.pojo.Task">
        SELECT t.task_id, t.task_name, t.description, ut.is_completed
        FROM tasks t
                 JOIN user_tasks ut ON t.task_id = ut.task_id
        WHERE ut.user_id = #{userId}

    </select>
    <select id="getuserById" resultType="io.github.sztuwebclub.webcheckin.pojo.Task">
        SELECT t.task_id, t.task_name, t.description, ut.is_completed
        FROM tasks t
                 JOIN user_tasks ut ON t.task_id = ut.task_id
        WHERE ut.user_id = #{userId}

    </select>
    <select id="updateUser" resultType="io.github.sztuwebclub.webcheckin.pojo.Task">
        SELECT t.task_id, t.task_name, t.description, ut.is_completed
        FROM tasks t
                 JOIN user_tasks ut ON t.task_id = ut.task_id
        WHERE ut.user_id = #{userId}

    </select>
</mapper>
